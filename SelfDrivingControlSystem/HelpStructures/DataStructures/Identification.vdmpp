class Identification
	types
		public ID_t = nat;
		
	values
		maxID : nat = 10000;						/* default 10.000 and cannot be changed because of const properties */
	
	instance variables
		private math: MATH;
		private id: ID_t;									/* Will become a constant after the constructor*/
	
	inv id > 0 and id < maxID;
	
	
	operations
	
		public Identification : () ==> Identification
			Identification() == id := generateID()
			post id > 0;
		
		pure public getID : () ==> ID_t
			getID() == return id;
			
		private generateID : () ==> ID_t
			generateID() == return math.rand(maxID-1)+1
			post RESULT > 0;

end Identification


/*function are static operators*/