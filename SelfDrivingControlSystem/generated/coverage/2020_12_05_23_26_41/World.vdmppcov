
class World
		operations
			public Run : () ==> ()
				Run() == (
					dcl cityMap : CityMap;
					
					IO`println("Hello, World");
					
					cityMap := CityMapFileFactory`loadCity("miniCityRoads", "miniCityNodeInfo");
					--CityMapFileFactory`saveCity("Test", "TestNodes", cityMap);
					IO`println("Hello, World");
				);

end World